# Incident: llama.cpp router maintenance (2026-09-18 21:00–22:17 local)

- Cells A-C1 (start 20:57:04) and A-C2 (start 21:37:04) launched into the maintenance window:
  pi -p processes hung at startup, run.log 0 bytes, killed by the 2400 s cell cap exactly
  (20:57:04→21:37:04, 21:37:04→22:17:05 — both 2400 s).
- Snapshots renamed *.VOID (grader only scans dirs containing TODO.md, so voids never score).
- B-C0 (start 22:17:05) launched after recovery; verified progressing (session jsonl growing).
- Remediation: re-run A-C1 and A-C2 sequentially with the same run_cell.sh after the grid finishes.

## Correction (post-forensics, 23:15)
Sessions show all three voided cells were ALIVE (agent actively generating):
- A-C1 session 96 KB, writes until 21:32 (kill 21:37); A-C2 67 KB, writes until 22:08 (kill 22:17); B-C0 59 KB, active until 22:53 (kill 22:57).
- So the primary failure mode was the 2400 s cell cap being too short for 27B's pace on these payloads (router maintenance likely slowed generation further, but did not freeze the runs).
- run.log is 0 bytes for killed cells because pi -p emits its final output only on clean exit.
- Remedy: cap raised to 3600 s in run_cell.sh (line 32). Re-runs: A-C1, A-C2, B-C0 (+ B-C1 if it hit the old cap at 23:37:05).

## Final state (00:50)
- All five original cells voided by caps; B-C0/B-C1/B-C2 agents were alive and productive throughout
  (B-C2's forensic probe.ts recovered from the live payload: full correct undici table,
  cross-verified against installed undici@8.10.2 source — see B-C2-*.VOID/probe.ts.forensic).
- Cap raised to 4800 s. Re-run chain launched: A-C1, A-C2, B-C0, B-C1, B-C2 (rerun_cells.sh, appends grid.log).
